// PRD: single product shadow only — never on cards, buttons, or text
export const shadows = {
  product: "rgba(0,0,0,0.22) 3px 5px 30px 0",
  card:    "0 1px 3px rgba(0,0,0,0.04), 0 1px 2px rgba(0,0,0,0.04)",
  none:    "none",
} as const;
