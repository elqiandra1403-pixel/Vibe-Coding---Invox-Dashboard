"use client";
// AppShell — TopNav + Sidebar wrapper for Admin/Finance roles
// Phase 2: Compose TopNav + Sidebar + main content area
export function AppShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
