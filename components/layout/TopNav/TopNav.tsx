"use client";
// TopNav — frosted-glass pinned header
// Design: backdrop-filter blur(20px) saturate(180%), --invox-color-parchment @ 80%
// Contains: Logo, GlobalSearch, primary nav links, NotificationBell, UserMenu, Create Invoice CTA
export function TopNav() {
  return null;
}
