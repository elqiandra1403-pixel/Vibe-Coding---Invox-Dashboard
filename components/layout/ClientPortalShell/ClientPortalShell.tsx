"use client";
// ClientPortalShell — simplified header for Client-role read-only portal
// Shows: Logo, invoice list nav, user greeting, sign out
export function ClientPortalShell({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
