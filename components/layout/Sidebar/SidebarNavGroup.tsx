// Sidebar navigation group (e.g., "Operational", "Insights", "Administration")
export function SidebarNavGroup({ title, children }: {
  title: string;
  children: React.ReactNode;
}) {
  return null;
}
