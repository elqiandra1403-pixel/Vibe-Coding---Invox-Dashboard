"use client";
// Sidebar — role-filtered navigation
// Active state: pill highlight in --invox-color-primary
// Reads nav config from config/navigation.ts, filtered by user role
export function Sidebar() {
  return null;
}
