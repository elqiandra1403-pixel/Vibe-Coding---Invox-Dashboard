"use client";
// Single sidebar navigation item with icon + label + active state
export function SidebarNavItem({ label, href, icon, isActive }: {
  label: string;
  href: string;
  icon: React.ReactNode;
  isActive: boolean;
}) {
  return null;
}
