"use client";
// SearchInput — pill-shaped search with debounce (300ms)
// Keyboard shortcut: / (desktop only)
export function SearchInput({ placeholder, onSearch }: {
  placeholder?: string;
  onSearch: (value: string) => void;
}) {
  return null;
}
