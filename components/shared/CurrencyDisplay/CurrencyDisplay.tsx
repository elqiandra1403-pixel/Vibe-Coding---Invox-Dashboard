// CurrencyDisplay — consistent currency formatting using org currency setting
export function CurrencyDisplay({ amount, currency = "USD" }: {
  amount: number;
  currency?: string;
}) {
  return null;
}
