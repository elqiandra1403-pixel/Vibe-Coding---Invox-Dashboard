import "server-only";
// React Email template: client portal invite
export function PortalInviteEmailTemplate({ customerName, inviteLink, orgName }: {
  customerName: string;
  inviteLink: string;
  orgName: string;
}) {
  return null;
}
