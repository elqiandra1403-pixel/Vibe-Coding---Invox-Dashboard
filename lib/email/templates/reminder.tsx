import "server-only";
// React Email template: payment reminder
export function ReminderEmailTemplate({ invoiceNumber, customerName, balanceDue, dueDate, portalLink }: {
  invoiceNumber: string;
  customerName: string;
  balanceDue: number;
  dueDate: string;
  portalLink: string;
}) {
  return null;
}
