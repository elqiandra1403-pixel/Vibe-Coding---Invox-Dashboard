import "server-only";
// React Email template: invoice delivery
// Phase 2: Implement using @react-email/components
// Includes: invoice summary, line items, total, secure portal link
export function InvoiceEmailTemplate({ invoiceNumber, customerName, total, portalLink }: {
  invoiceNumber: string;
  customerName: string;
  total: number;
  portalLink: string;
}) {
  return null;
}
